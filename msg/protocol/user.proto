syntax = "proto3";
package message;

option go_package=".;gen";

import "msg/protocol/options.proto";

message User {
    string name  = 1;
}

message CSTest {
    option (msgid) = 101;
    string code = 1;
}